# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool:
  vmImage: ubuntu-latest

variables:
- group: UpdateManagerGOLD

steps:
- checkout: self


- task: AzureResourceManagerTemplateDeployment@3
  inputs:
      deploymentScope: 'Resource Group'
      azureResourceManagerConnection: 'anugrah pay as you go(1)(cbf8b966-9488-4fc7-a133-b65c3b0285a8)'
      subscriptionId: 'cbf8b966-9488-4fc7-a133-b65c3b0285a8'
      action: 'Create Or Update Resource Group'
      resourceGroupName: '$(ResourceGroup)'
      location: '$(Location)'
      templateLocation: 'Linked artifact'
      csmFile: '$(System.DefaultWorkingDirectory)/_swapnilmore2901_UpdateManagerFinal1/GOLD/template.json'
      csmParametersFile: '$(System.DefaultWorkingDirectory)/_swapnilmore2901_UpdateManagerFinal1/GOLD/parameters.json'
      deploymentMode: 'Incremental'
